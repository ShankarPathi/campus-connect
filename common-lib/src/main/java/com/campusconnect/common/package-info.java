/**
 * Campus Connect shared domain core.
 *
 * <p>Skeleton package created in Story 1.1. Subsequent stories populate sub-packages:
 * <ul>
 *   <li>{@code tenancy}  — TenantContext + TenantAwareRepository (Story 1.3)</li>
 *   <li>{@code security} — JwtService + SecurityConfig (Story 1.4)</li>
 *   <li>{@code web}      — ApiResponse + GlobalExceptionHandler (Story 1.2)</li>
 *   <li>{@code domain}, {@code repository}, {@code eligibility}, {@code events},
 *       {@code statemachine}, {@code file}, {@code config} — later stories</li>
 * </ul>
 */
package com.campusconnect.common;
